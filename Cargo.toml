[package]
name = "ravenlock"
version = "1.0.0"
edition = "2021"
authors = ["xtr4ng3"]
description = "Local folder integrity sentinel with canary files, baseline scanning and defensive reports."
license = "MIT"

[profile.release]
opt-level = "z"
lto = true
codegen-units = 1
strip = true
